# 🚀 Быстрый старт IntegrityOS

## Что уже готово:
- ✅ Backend код (FastAPI + PostgreSQL)
- ✅ Frontend код (React + TypeScript)
- ✅ Тестовые данные (99 объектов, 347 обследований)
- ✅ Docker конфигурация
- ✅ Git репозиторий инициализирован

## Вариант 1: Запуск с Docker (Самый простой) 🐳

### Шаг 1: Создайте .env файл
```bash
# Скопируйте пример
copy .env.example .env

# Для frontend
copy frontend\.env.example frontend\.env
```

### Шаг 2: Запустите Docker Compose
```bash
docker-compose up -d
```

### Шаг 3: Откройте приложение
- Frontend: http://localhost:5173
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/docs

### Шаг 4: Импортируйте данные
1. Откройте http://localhost:5173/import
2. Загрузите файлы из `backend/data/`:
   - Сначала `Objects.csv`
   - Потом `Diagnostics.csv`

---

## Вариант 2: Локальный запуск (Для разработки) 💻

### Требования:
- Python 3.11+
- Node.js 18+
- PostgreSQL 15+

### Backend

```bash
# 1. Перейдите в backend
cd backend

# 2. Создайте виртуальное окружение
python -m venv venv
venv\Scripts\activate

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Создайте базу данных PostgreSQL
# Установите PostgreSQL и создайте БД:
createdb integrityos

# 5. Создайте .env файл
copy .env.example .env
# Отредактируйте DATABASE_URL в .env

# 6. Запустите сервер
uvicorn app.main:app --reload
```

Backend будет доступен на http://localhost:8000

### Frontend

```bash
# 1. Перейдите в frontend (в новом терминале)
cd frontend

# 2. Установите зависимости
npm install

# 3. Создайте .env файл
copy .env.example .env

# 4. Запустите dev-сервер
npm run dev
```

Frontend будет доступен на http://localhost:5173

---

## Тестирование функций

### 1. Импорт данных
- Перейдите на страницу "Импорт"
- Загрузите `backend/data/Objects.csv`
- Загрузите `backend/data/Diagnostics.csv`

### 2. Просмотр дашборда
- Откройте главную страницу
- Увидите статистику и графики

### 3. Карта
- Откройте страницу "Карта"
- Увидите объекты на карте Казахстана
- Маркеры окрашены по уровню риска

### 4. Список объектов
- Откройте "Объекты"
- Поиск и фильтрация
- Клик на объект для деталей

### 5. Отчеты
- Откройте "Отчеты"
- Выберите формат (HTML/PDF)
- Нажмите "Сгенерировать"

---

## Полезные команды

### Docker
```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Логи
docker-compose logs -f

# Перезапуск
docker-compose restart
```

### Git
```bash
# Статус
git status

# Добавить изменения
git add .
git commit -m "Описание"

# Загрузить на GitHub (после настройки remote)
git push
```

### Backend
```bash
# Генерация новых данных
python scripts/data_generator.py

# Запуск тестов (когда будут добавлены)
pytest tests/
```

### Frontend
```bash
# Разработка
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр сборки
npm run preview
```

---

## Проблемы и решения

### "Port already in use"
```bash
# Остановите процесс на порту 8000 или 5173
# Windows:
netstat -ano | findstr :8000
taskkill /PID <PID> /F
```

### "Database connection failed"
- Убедитесь что PostgreSQL запущен
- Проверьте DATABASE_URL в .env
- Создайте базу данных: `createdb integrityos`

### "Module not found"
```bash
# Backend
pip install -r requirements.txt

# Frontend
npm install
```

---

## Следующие шаги

1. ✅ Протестируйте все функции
2. ✅ Загрузите на GitHub
3. ✅ Подготовьте презентацию
4. ✅ Дождитесь реальных данных от организаторов

---

**Готово к демонстрации! 🎉**
